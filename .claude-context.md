# Worktree Context

| Field | Value |
|-------|-------|
| Created | 2026-05-01 11:07 KST |
| Branch | `cursor/lumi-planner-preview` |
| Remote | `origin git@github.com:ssoso27/purple-joonsik.git` |
| Status | In Progress |
| Last Session | 2026-05-01 11:07 KST |

## Work Summary

Lumi Planner 데스크톱 앱 메인 화면 미리보기를 React/Vite/Electron으로 구현했다. 사용자가 제공한 플래너 레퍼런스 이미지를 기준으로 사이드바, 오늘 할 일, 진행률/포인트 카드, 메모, 최근 기록, 하단 캐릭터 채팅 영역을 구성했다.

레퍼런스와 현재 렌더 캡처는 프로젝트 안에 저장되어 있다.

- Reference: `docs/reference/lumi-planner-main-reference.png`
- Latest render: `docs/reference/lumi-planner-rendered-current.png`

## Git State

Current branch tracks `origin/cursor/lumi-planner-preview`.

Recent commits:

- `932b6ad Create Lumi Planner preview app`
- `7c5a1f2 Strengthen UI review guidelines`

## What's Done

- [x] `cursor/lumi-planner-preview` 브랜치 생성
- [x] 초기 Lumi Planner 미리보기 앱 커밋
- [x] GitHub 원격 `git@github.com:ssoso27/purple-joonsik.git` 등록
- [x] 브랜치 푸시 완료
- [x] UI 작업 지침 강화: `AGENTS.md`, `docs/ui-agent-guideline.md`
- [x] 레퍼런스 원본과 렌더 캡처 저장

## Critical UI Rules

- 레퍼런스의 정보량은 디자인 요소다. 할 일 개수, 완료 상태, 진행률, 포인트, 날짜, 메모 줄 수, 캐릭터 대사는 사용자 승인 없이 바꾸면 안 된다.
- 화면이 잘린다고 해서 리스트 개수나 문구를 줄이면 안 된다. 레이아웃 문제는 프레임 크기, 행 높이, 간격, 스케일, 배치 규칙으로 해결한다.
- 렌더 검증 시 반드시 수정 대상 요소와 전체 화면을 둘 다 레퍼런스와 비교한다.
- 원형 진행률, 캐릭터, 코인, 아이콘은 찌그러지면 안 된다. `aspect-ratio`, 고정 width/height, `flex-shrink: 0` 등으로 비율을 보호한다.
- 이전 Vite 포트/캐시 화면을 보고 검증하면 안 된다. 실제 최신 코드가 렌더되는 포트를 확인해야 한다.

## Mistakes To Avoid

- 채팅창 잘림을 해결하려고 할 일 5개를 3개로 줄였던 것은 잘못이었다. 레퍼런스 훼손이므로 반복 금지.
- `60% / 3 / 5 / 120P`를 `67% / 2 / 3 / 80P`로 바꿨던 것은 잘못이었다. 레퍼런스 숫자와 밀도 유지.
- 카드 높이를 과하게 압축해 `.progress-ring`이 세로로 눌렸던 문제가 있었다. 진행률 원형은 반드시 정원이어야 한다.
- 부분 요소만 보고 "보인다"고 말하면 안 된다. 전체 화면 리듬, 여백, 하단 채팅 영역까지 함께 리뷰해야 한다.

## Current Visual Notes

- `.progress-ring`은 5개/60% 데이터로 복원했고, CSS에서 정원 비율 보호를 추가했다.
- 하단 채팅 영역은 보이지만, 전체 레퍼런스 대비 최근 기록과 채팅 영역 사이 리듬은 추가 조정 여지가 있다.
- 캐릭터는 CSS 기반 임시 구현이다. 공식 캐릭터/픽셀 스프라이트가 있으면 교체 필요.
- 전체 가로 폭은 레퍼런스보다 넓게 느껴질 수 있다. 이후 작업은 레퍼런스 저장 이미지와 최신 렌더 이미지를 나란히 비교하며 진행할 것.

## Next Steps

- [ ] 최신 렌더에서 `.progress-ring` bounds가 정사각형인지 다시 확인
- [ ] 하단 채팅 영역과 최근 기록 사이 간격을 레퍼런스 기준으로 조정
- [ ] 조정 후 `docs/reference/lumi-planner-rendered-current.png` 갱신
- [ ] `npm run build`, `npm run lint` 실행
- [ ] 수정 대상 리뷰 + 전체 화면 리뷰를 분리해서 보고

## Session Log

- 2026-05-01 11:07: Lumi Planner UI 구현, 레퍼런스 저장, 지침 강화, 원격 푸시 상태를 컨텍스트로 기록
